(* Simple arithmetic expression grammar *)
(* Demonstrates basic EBNF with proper precedence *)

expr = <ws>* term ( <ws>* addop <ws>* term )* <ws>* ;

term = factor ( <ws>* mulop <ws>* factor )* ;

factor = number
       | <"("> <ws>* expr <ws>* <")"> ;

addop = "+" | "-" ;
mulop = "*" | "/" ;

number = #"[0-9]+(\.[0-9]+)?" ;
ws = #"\s+" ;
